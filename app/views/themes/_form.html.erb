<%= form_with(model: theme) do |form| %>
  <% if theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
        <% theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :theme_title, style: "display: block" %>
    <%= form.text_field :theme_title %>
  </div>

  <div>
    <%= form.label :theme_descr, style: "display: block" %>
    <%= form.text_field :theme_descr %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
